from setuptools import setup

setup(
    name="goal-tracking-env",
    version="0.1.0",
    py_modules=["goal_tracking_env"],
    install_requires=["numpy", "opencv-python"],
    python_requires=">=3.8",
)
